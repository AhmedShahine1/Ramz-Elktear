@model IEnumerable<SubCategoryDTO>

<div class="headerIndex">
    <h3><span class="text-truncate">SubCategory List</span></h3>
    <a href="@Url.Action("Create")" class="btn btn-success mb-3">
        <i class="bx bx-plus"></i> <span class="text-truncate">Add New SubCategory</span>
    </a>
</div>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>#</th>
            <th><span class="text-truncate">Name (Arabic)</span></th>
            <th><span class="text-truncate">Name (English)</span></th>
            <th><span class="text-truncate">Category Name</span></th>
            <th><span class="text-truncate">Brand Name</span></th>
            <th><span class="text-truncate">Active</span></th>
            <th><span class="text-truncate">Actions</span></th>

        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            int index = 1;
            foreach (var subCategory in Model)
            {
                <tr>
                    <td>@index</td>
                    <td>@subCategory.NameAr</td>
                    <td>@subCategory.NameEn</td>
                    <td>@subCategory.Category.NameEn</td>
                    <td>@subCategory.Brand.NameEn</td>
                    <td>
                        @if (subCategory.IsActive)
                        {
                            <span class="badge bg-label-primary"><i class="bx bx-check"></i> Active</span>
                        }
                        else
                        {
                            <span class="badge bg-label-danger"><i class="bx bx-x"></i> Inactive</span>
                        }
                    </td>
                    <td>
                        <div class="dropdown">
                            <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                <i class="bx bx-dots-vertical-rounded"></i>
                            </button>
                            <div class="dropdown-menu">
                                <a class="dropdown-item" href="@Url.Action("Edit", new { id = subCategory.Id })">
                                    <i class="bx bx-edit-alt me-1"></i> Edit
                                </a>
                                <form action="@Url.Action("Delete", new { id = subCategory.Id })" method="post" class="dropdown-item">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn p-0" style="border: none; background: transparent; color: inherit;">
                                        <i class="bx bx-trash me-1"></i> Delete
                                    </button>
                                </form>
                            </div>
                        </div>
                    </td>
                </tr>
                index++;
            }
        }
        else
        {
            <tr>
                <td colspan="7" class="text-center">No subcategories available.</td>
            </tr>
        }
    </tbody>
</table>
