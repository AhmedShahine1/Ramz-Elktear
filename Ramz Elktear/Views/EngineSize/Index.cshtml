@model IEnumerable<EngineSizeDTO>

<div class="headerIndex">
    <h3><span class="text-truncate">@Localizer["EngineSizeList", "EngineSize"]</span></h3>
    <a href="@Url.Action("Create")" class="btn btn-success mb-3">
        <i class="bx bx-plus"></i> <span class="text-truncate">@Localizer["AddNewEngineSize", "EngineSize"]</span>
    </a>
</div>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>#</th>
            <th><i class="bx bx-text"></i> <span class="text-truncate">@Localizer["NameArabic", "EngineSize"]</span></th>
            <th><i class="bx bx-text"></i> <span class="text-truncate">@Localizer["NameEnglish", "EngineSize"]</span></th>
            <th><i class="bx bx-info-circle"></i> <span class="text-truncate">@Localizer["DescriptionArabic", "EngineSize"]</span></th>
            <th><i class="bx bx-info-circle"></i> <span class="text-truncate">@Localizer["DescriptionEnglish", "EngineSize"]</span></th>
            <th><i class="bx bx-image"></i> <span class="text-truncate">@Localizer["Image", "EngineSize"]</span></th>
            <th><i class="bx bx-check-circle"></i> <span class="text-truncate">@Localizer["Active", "EngineSize"]</span></th>
            <th><i class="bx bx-cog"></i> <span class="text-truncate">@Localizer["Actions", "EngineSize"]</span></th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            int index = 1;
            foreach (var engineSize in Model)
            {
                <tr>
                    <td>@index</td>
                    <td>@engineSize.NameAr</td>
                    <td>@engineSize.NameEn</td>
                    <td>@engineSize.DescriptionAr</td>
                    <td>@engineSize.DescriptionEn</td>
                    <td>
                        @if (!string.IsNullOrEmpty(engineSize.ImageUrl))
                        {
                            <img src="@engineSize.ImageUrl" alt="@Localizer["EngineSizeImage", "EngineSize"]" width="60" class="rounded shadow" />
                        }
                        else
                        {
                            <i class="bx bx-image-alt text-muted"></i> <p>@Localizer["NoImage", "EngineSize"]</p>
                        }
                    </td>
                    <td>
                        @if (engineSize.IsActive)
                        {
                            <span class="badge bg-label-primary">
                                <i class="bx bx-check"></i> @Localizer["Active", "EngineSize"]
                            </span>
                        }
                        else
                        {
                            <span class="badge bg-label-danger">
                                <i class="bx bx-x"></i> @Localizer["Inactive", "EngineSize"]
                            </span>
                        }
                    </td>
                    <td>
                        <div class="dropdown">
                            <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                <i class="bx bx-dots-vertical-rounded"></i>
                            </button>
                            <div class="dropdown-menu">
                                <a class="dropdown-item" href="@Url.Action("Edit", new { id = engineSize.Id })">
                                    <i class="bx bx-edit-alt me-1"></i> @Localizer["Edit", "EngineSize"]
                                </a>
                                <form action="@Url.Action("Delete", new { id = engineSize.Id })" method="post" class="dropdown-item">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="btn p-0" style="border: none; background: transparent; color: inherit;">
                                        <i class="bx bx-trash me-1"></i> @Localizer["Delete", "EngineSize"]
                                    </button>
                                </form>
                            </div>
                        </div>
                    </td>
                </tr>
                index++;
            }
        }
        else
        {
            <tr>
                <td colspan="8" class="text-center">@Localizer["NoEngineSizesAvailable", "EngineSize"]</td>
            </tr>
        }
    </tbody>
</table>